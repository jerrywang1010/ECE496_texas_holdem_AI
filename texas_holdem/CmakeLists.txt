cmake_minimum_required(VERSION 3.21.2)
set(CMAKE_CXX_STANDARD 14)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
project(main VERSION 1.0)

set(Boost_USE_STATIC_LIBS OFF) 
set(Boost_USE_MULTITHREADED ON)  
set(Boost_USE_STATIC_RUNTIME OFF) 
find_package(Boost REQUIRED COMPONENTS filesystem regex) 

if(Boost_FOUND)
    include_directories(${Boost_INCLUDE_DIRS}) 
    add_executable(main "main.cpp" "player.cpp" "game.cpp" "concrete_game_state.cpp" "gametree.cpp" "treenode.cpp") 
    target_include_directories(main PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
    target_link_libraries(main ${Boost_LIBRARIES} ${CMAKE_SOURCE_DIR}/OMPEval-master/lib/ompeval.a)
endif()

# add_executable ("${PROJECT_NAME}" "main.cpp" "player.cpp" "game.cpp" "concrete_game_state.cpp")
# target_link_libraries(main ${CMAKE_SOURCE_DIR}/OMPEval-master/lib/ompeval.a)

#install(TARGETS "${PROJECT_NAME}" DESTINATION bin)
#install(FILES "main.cpp" DESTINATION src)

# target_include_directories(main PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
